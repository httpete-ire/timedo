import React from 'react';

const Timer = () => {
  return (
    <div className="timer-controls__container">
      <button>Start</button>
      <button>Reset</button>
    </div>
  )
};

export default Timer;
